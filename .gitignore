# Carpeta que genera "Publicar": es el resultado de compilar, no código fuente.
# Se vuelve a generar cuando se necesite, no tiene que estar en el repositorio.
/Publicado/
/Entrega/
/SistemaHotel.zip

# Archivos temporales de los editores
.vs/
.idea/
